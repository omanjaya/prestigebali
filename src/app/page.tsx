// Etalase (showroom, browse-first) — placeholder. UI diisi saat implementasi.

export default function HomePage() {
  return (
    <main style={{ maxWidth: 720, margin: "0 auto", padding: "4rem 1.5rem" }}>
      <h1>Prestige</h1>
      <p>Sewa mobil mewah — lepas kunci atau pakai sopir.</p>
      <p style={{ color: "#666" }}>
        Etalase (showroom) akan tampil di sini. Skeleton proyek siap; logika domain dibangun
        test-first lewat <code>/tdd</code> di <code>src/domain/booking</code>.
      </p>
    </main>
  );
}
